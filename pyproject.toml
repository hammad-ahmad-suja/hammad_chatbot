[project]
name = "hammad-chatbot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.3",
    "ollama>=0.6.2",
    "streamlit>=1.64.0",
]

[project.scripts]
hammad-chatbot = "hammad_chatbot:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
